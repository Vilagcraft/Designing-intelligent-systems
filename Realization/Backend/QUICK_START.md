# Быстрый старт Backend

## ✅ Исправлены проблемы с импортами

Все импорты изменены с абсолютных на относительные:
- `from Realization.Backend.app...` → `from app...`

## 🚀 Запуск сервера

### Вариант 1: Через виртуальное окружение (рекомендуется)

```bash
# Перейдите в папку Backend
cd Realization/Backend

# Создайте виртуальное окружение (если ещё не создано)
python -m venv .venv

# Активируйте его
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Запустите сервер
python app/main.py
```

### Вариант 2: Через uvicorn напрямую

```bash
cd Realization/Backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## 📌 Проверка работы

После запуска откройте:
- Главная страница: http://localhost:8000
- API документация: http://localhost:8000/docs
- Проверка здоровья: http://localhost:8000/health

## 🔧 Новые эндпоинты

### POST /dataset/analyze
Анализирует датасет отзывов
- Форматы: CSV, JSON, Parquet
- Обязательная колонка: одна из `clean_text`, `review_text`, `review`, `text`, `content`
- Макс. размер: 100MB

Пример использования через curl:
```bash
curl -X POST http://localhost:8000/dataset/analyze \
  -F "file=@your_dataset.csv"
```

## ⚠️ Требования

1. **Python 3.8+**
2. **Обученная модель**: Перед использованием /dataset/analyze нужно обучить модель через `/train`
3. **Зависимости**: Все из `requirements.txt`

## 🐛 Решение проблем

### ModuleNotFoundError: No module named 'app'
Убедитесь, что запускаете из правильной директории:
```bash
cd Realization/Backend
python app/main.py  # НЕ python main.py
```

### Model not found
Сначала обучите модель:
1. Откройте http://localhost:8000/docs
2. Найдите POST /train
3. Нажмите "Try it out" → "Execute"
4. Дождитесь завершения обучения

### CORS errors
CORS уже настроен для всех источников в production режиме. Если нужно ограничить:
```python
# В app/main.py измените:
allow_origins=["http://localhost:5173"]  # Только фронтенд
```

## 📦 Структура проекта

```
Backend/
├── app/
│   ├── main.py              # Точка входа FastAPI
│   ├── config.py            # Конфигурация путей
│   ├── router_*.py          # API роутеры
│   ├── models/              # Pydantic модели
│   ├── services/            # Бизнес-логика
│   └── nn/                  # Neural Network код
├── logs/                    # Логи обучения и анализа
├── requirements.txt         # Зависимости
└── .venv/                   # Виртуальное окружение
```

